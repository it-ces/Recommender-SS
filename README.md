# Recommeder 

You need install vagrant and virtualbox to run the 
app in a ubuntu virtual machin# Recommeder 



in terminal you need:
   vagrant up
   vagrant ssh
   cd /vagrant 
   
now you are in the share folder of local and virtual machine.
